name: CI

on:
  workflow_dispatch:
  push:
    branches: [source]
    paths-ignore:
      - LICENSE
      - '**/*.md'

jobs:
  version:
    permissions:
      contents: write
    uses: katastroma/skeuotheke/.github/workflows/version.yaml@v0
    with:
      GH_APP_ID: ${{ vars.PLATFORM_GH_APP_CLIENT_ID }}
    secrets:
      GH_APP_PRIVATE_KEY: ${{ secrets.PLATFORM_GH_APP_PRIVATE_KEY }}
